{
  "name": "gluwasdk.testsuite",
  "version": "1.0.0",
  "description": "Unit Test for Gluwa SDK",
  "main": "setup.js",
  "scripts": {
    "test": "mocha --timeout 30000 --retries 2",
    "test-with-reporter": "mocha --timeout 30000 --reporter mocha-junit-reporter --reporter-options outputs=true --retries 2"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^8.4.0",
    "mocha-junit-reporter": "^2.0.0"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "crypto-js": "^4.0.0",
    "dotenv": "^9.0.2",
    "ethers": "^5.1.4",
    "js-base64": "^3.6.0"
  }
}
